# EC2 instance, role and cfn-signal

Creates an EC2 instance, a role that allows SSM access to that instance, and uses cfn-signal in the EC2 userdata and Cloudformation template to signal completion of the stack
